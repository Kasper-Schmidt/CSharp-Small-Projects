using System;

namespace classes
{
    class Program
    {
        static void Main(string[] args)
        {
            // classes = A bundle of related code
            //           Can be used as a bluerint to create objects (OOP)
            //           Check the file Messages.cs

            Messages.Hello();
            Messages.Waiting();
            Messages.Bye();











            Console.ReadKey();
        }
    }
}